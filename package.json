{
  "name": "oschat",
  "version": "0.1.0",
  "private": true,
  "description": "Realtime chat app - monorepo (Next.js + Express + Socket.io)",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run -w apps/web dev & npm run -w apps/server dev",
    "build": "npm run -w apps/web build && npm run -w apps/server build",
    "start": "npm run -w apps/server start",
    "lint": "echo 'no lints yet'",
    "typecheck": "echo 'no types yet'"
  },
  "author": "demonkiller",
  "license": "ISC"
}
