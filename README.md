# oschat (monorepo)

Realtime chat app monorepo targeting Next.js (web) + Express/Socket.io (server) with MongoDB and Redis.

Structure:

```
apps/
  web/
  server/
packages/
  shared/
infra/
```

Dev commands:

```
npm install
npm run dev
```
